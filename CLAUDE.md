# LastSight AI — 프로젝트 지침 (SuperB AI × BDAI 해커톤)

이 파일은 이 프로젝트 폴더에서 작업할 때 항상 따라야 할 규칙과 맥락을 담고 있다.
세부 배경 자료는 아래 파일을 참고한다 (필요할 때만 열어서 확인, 매번 전체를 다시 읽지 않아도 됨):

- `docs/LastSight_AI_최종기획서.html` — 프로젝트 전체 기획서 (문제 정의, 시나리오, 클래스 정의, 지표, MVP 범위, 발표 구성 등 25개 섹션)
- `docs/Superb_AI_X_BDAI_해커톤_과제_정의.pdf` — 해커톤 공식 과제 정의 (1~5p 확보, 6~13p는 미확보 상태 — 심사 기준·제출 방법·일정이 나올 경우 사용자에게 추가로 확인)
- `docs/bdai-platform-guide-study.md` — BDAI 플랫폼 사용 매뉴얼 전체 39페이지 요약본 (화면별 기능, 용어, SDK 사용법)
- `docs/bdai-platform-guide-beginner.md` — 위 매뉴얼의 완전 초보자용 버전
- `docs/development_log.md` — 개발 과정 기록 (발표자료·질의응답 대비용). **의미 있는 작업 단계(결정·구현·문제해결)가 끝날 때마다 이 파일에 이어서 기록할 것** — 무엇을 했는지, 왜 그렇게 했는지, 막힌 부분은 어떻게 풀었는지 순서대로

프로젝트 폴더 구조와 각 디렉터리의 역할은 `README.md`를 참고한다.

---

## 1. 프로젝트 한 줄 요약

**LastSight AI**: RGB CCTV를 활용해 **평상시에는 산업현장의 화재·안전사고를 예방**(PPE 착용 확인)하고, **비상시에는 연기·불꽃을 감지해 화재경보를 자동 트리거한 뒤 매초 그 프레임을 다시 확인**해 "지금 이 순간 몇 명이 보이고, 그중 쓰러졌거나 연기에 둘러싸인 사람이 있는지"를 시간순 타임라인과 카드로 쌓아주고, 지금까지의 기록을 근거로 자유롭게 물어볼 수 있는 구조 브리핑 챗봇까지 제공하는 통합 산업안전 비전 시스템(개인별 신원·체류시간 추적은 하지 않음 — 아래 2026-08-04 메모 참고).

> ⚠️ **2026-07-28 방향 전환**: 원래 `docs/LastSight_AI_최종기획서.html`은 "화재를 발견하는 AI가 아니라"를 핵심 차별점으로 내세우며 화재신호를 관리자가 수동 입력하는 것으로 설계했었다. 이후 논의를 거쳐 **화재/연기 자동 탐지를 시스템에 포함**시키는 것으로 방향을 바꿨다. 차별점은 "화재 탐지에서 그치지 않고, 그 이후 대피 이행까지 끝까지 확인하는 AI"로 재정의한다 (화재 탐지 자체보다 그 이후 정보 공백 해소가 여전히 핵심 가치).
>
> ✅ **2026-07-31 동기화 완료**: `docs/LastSight_AI_최종기획서.html`을 이 CLAUDE.md 기준(관측지속시간 모델, 평상시/비상시 두 축, detection 전환, 차별화 기능 3종 포함)에 맞춰 전면 재작성했다 — 더 이상 낡은 버전이 아니며, 발표자료·제출보고서 작성 시 그대로 참고할 수 있다.
>
> ⚠️ **2026-07-30 방향 확장 (Superb AI 멘토링 피드백 반영, `docs/Superb_AI_공모전_멘토링_QnA.md` 참고)**: "화재 이후 잔류인원 추적"에만 집중하면 데이터 부족·인식 오류로 구현 난도가 지나치게 높아진다는 멘토 피드백에 따라, **평상시 예방 기능을 프로젝트의 정식 축으로 확장**한다. 기존 "비상시 대응"(화재 탐지→추적→상태 판정→구조카드) 축은 그대로 유지하되, 그 앞단에 "평상시 안전관리"(PPE 상시 점검) 축을 더해 두 축으로 프로젝트를 구성한다. 잔류인원 관련 정보는 여전히 "확정된 사실"이 아니라 **보조 정보로만 제공**한다는 원칙은 그대로 유지 — 오히려 이번 피드백으로 이 원칙이 왜 중요한지가 재확인됐다.
>
> ⚠️ **2026-07-30 핵심 파이프라인 전환 (출구 통과 판정 폐기 → 관측 지속시간 모델)**: 기존 "출구 가상선 통과 판정" 방식은 카메라 시야가 출구 바깥까지 잡지 못하는 경우(실제 현장에서 흔함) "통과 확인" 자체가 구조적으로 영원히 성립할 수 없다는 한계가 드러났다 (`docs/development_log.md` 17번 참고). 이에 따라 출구 통과·대피 완료 판정 기능을 전면 폐기하고, **화재경보 발생 이후 각 작업자가 지금도 관측되고 있는지·얼마나 오래 관측되고 있는지**만 보는 방식으로 대체했다 (`docs/development_log.md` 18번 참고). ~~상태값은 `inside_observed`/`prolonged_presence`/`tracking_lost`/`camera_failure` 4가지이며, 화재경보 후 기본 5분을 넘도록 관측되면 `prolonged_presence`로 전환해 강조 표시한다.~~ → 이 개인별 상태 모델은 아래 2026-08-04 메모에서 다시 한번 폐기됐다.
>
> ⚠️ **2026-08-04 두 번째 파이프라인 전환 (개인별 추적·상태값 전면 폐기 → 화재 후 매초 2차 확인 카드)**: 위 관측 지속시간 모델(개인별 `inside_observed`/`prolonged_presence`/`tracking_lost`/`camera_failure` 4상태 + ByteTrack 추적)을 실제로 데모 영상에 돌려보니, 화면을 벗어났다 들어온 사람에게 예전 트랙 ID가 잘못 재부착되는 문제가 또 발견됐다(45번에서 고친 것으로도 완전히는 못 막는 사례, `docs/development_log.md` 51번 참고). 신원 특정을 안 하는 이 시스템 설계상 완벽한 재식별은 원천적으로 불가능하다는 결론을 내리고, **화재 발생 이후 개인별 추적·상태 판정 자체를 걷어냈다**. 대신:
> - 화재 모드에서는 감지된 사람 전원을 개인별 상태 구분 없이 **초록 박스 하나로만** 표시한다.
> - 화재경보가 뜬 순간부터 **매초** 그 프레임을 ZERO로 2차 확인해 "체류중/쓰러진 사람/연기에 둘러싸인 사람" 카드를 하나씩 쌓는다 — 누적 추정이 아니라 "그 순간의 사실 기록"이라는 관점(프레임마다 새로 매기는 합성 ID를 쓰므로 재부착 문제 자체가 발생할 수 없는 구조).
> - PPE 위반은 화재 발생 여부와 무관하게 이어지는 하나의 타임라인 위에서 화재 발생 노드·2차 확인 이정표와 함께 표시한다(관리자가 위반 카드를 열어 착용/미착용을 직접 정정 가능, 원판정은 감사 기록으로 유지).
> - `WorkerEvent`(`inside_observed`/`prolonged_presence`/`tracking_lost`/`camera_failure`), ByteTrack 추적, 상태 규칙 엔진, 확인 우선순위 트리아지는 **더 이상 실시간 파이프라인에서 쓰지 않는다**(코드는 vestigial로 남아있고 단위 테스트도 유지되지만 화면에는 안 나옴). "관측이 끊겼다고 대피 완료로, 계속 관측된다고 위험 확정으로 간주하지 않는다"는 절대 원칙 자체는 그대로 유지되며, 이제 그 원칙이 개인별 상태값이 아니라 "2차 확인 카드가 확정 선언이 아니라는 것"으로 구현된다. 세부 반영 내용은 2번(절대 원칙), 3번(시스템 구성), 4-3번(이벤트 라벨), 6번(MVP 범위) 참고.
>
> ⚠️ **2026-08-04 배포·부가 기능**: 구역(zones) 폴리곤을 관리자가 카메라 화면에서 직접 그리고 이름 붙이는 편집 UI, 지금까지 기록된 데이터(화재경보·구역인원·2차 확인·PPE 위반)를 근거로 Gemini가 쓰는 "요약 브리핑" 버튼(BDAI 테넌트에 이미 연결된 Gemini 게이트웨이 재사용, 안전 원칙을 시스템 프롬프트에 강제)을 추가했고, Render에 외부 접속 가능한 데모를 배포했다(`docs/development_log.md` 54·56·58번 참고). 사고 리플레이는 백엔드 API는 있지만 프론트엔드 화면은 아직 안 만들었다.
>
> ⚠️ **2026-08-05 요약 브리핑 → 대화형 챗봇으로 전환**: 한 번 누르면 headline+points를 한 번만 반환하던 "요약 브리핑" 버튼을, 영상 옆에 상시 배치된 채팅 패널로 바꿨다(`app/inference/briefing.py`의 `answer_chat_message`, `POST /situation-chat`) — 관리자가 입력창에 자유 질문("74초쯤 무슨 일이 있었어?", "오늘 안전장비 위반 몇 건이야?")을 하면 답한다. 처음엔 패널이 뜨자마자 자동으로 요약 질문을 한 번 보냈는데, 시나리오를 시작할 때마다 원치 않는 API 호출이 발생한다는 사용자 피드백으로 없앴다 — 이제 패널은 빈 채로 뜨고, 관리자가 직접 물어봐야 답이 나온다. 질문에서 시간 힌트(경과초·화재 기준·시각)를 정규식으로 인식해 그 시점에 저장된 프레임(화재 전후 상관없이 `data/demo/scenario.json`의 사전계산 프레임까지 포함)을 찾아 답변에 곁들인다. 실측으로 드러난 문제 두 가지를 고쳤다: ① 챗봇에 넘기는 시각이 전부 UTC라 화면(KST)과 9시간 어긋났던 것 → 전부 KST로 통일, ② "마크다운 쓰지 마라" 프롬프트 지시를 Gemini가 완벽히 지키지 않아 `**`/`#`/`---`/번호 목록이 섞여 나오던 것 → 프롬프트만 믿지 않고 응답을 후처리로 한 번 더 걷어내는 안전망(`_strip_markdown`)을 추가.

- 트랙: 산업안전·물류
- 팀 구성: 2인 — **A. 데이터/라벨링 담당**(BDAI 플랫폼 중심), **B. 모델/서비스 개발 담당**(VSCode 중심). 실제로는 두 역할이 계속 맞물리므로 아래 파이프라인 단계마다 누가 주도하는지 표시해 둠.

---

## 2. 절대 원칙 (코드·문서·발표 어디서든 위반 금지)

기획서에 명시된 안전·개인정보 설계는 이 프로젝트의 핵심 차별점이자 심사 포인트다. 기능 구현, 텍스트 작성(UI 문구, 로그, 발표자료 포함) 어디에서도 아래를 어겨선 안 된다.

- **관측이 끊겼다고 "대피 완료"로, 계속 관측된다고 "위험 확정"으로 간주하지 않는다.** 이 시스템은 출구 통과·대피 완료를 확정 선언하는 기능을 두지 않는다. 화재경보 발생 시점부터 매초 그 프레임을 2차 확인해 "지금 이 순간 무엇이 보이는지"만 카드로 남기며(개인별 관측 지속시간을 추적하지 않음 — 2026-08-04 메모 참고), 화면 밖으로 나간 사람을 안전/위험 어느 쪽으로도 해석하지 않는다.
- **2차 확인에서 "쓰러진 사람"·"연기에 둘러싸인 사람" 같은 우려 신호가 잡히면 대시보드에서 강조 표시한다.** 다만 이 표시도 "구조 필요 확정"이 아니라 "확인이 필요하다"는 신호일 뿐이며 관리자 판단을 대체하지 않는다 — 카드 상단에 항상 "추정 정보 — 확정 아님"을 노출한다.
- **얼굴인식·사번 매칭·신원 특정 금지**. 국적·인종·성별·나이·체형 추정 로직이나 UI 문구를 절대 추가하지 않는다. 사람은 임시 추적 ID로만 다룬다.
- **동일 복장 = 동일인이 아니다**. 복장(색상 등)만으로 두 사람을 같은 ID로 병합하는 로직을 넣지 않는다.
- **공장 전체 재실 인원을 확정하지 않는다.** "CCTV 관측 범위 내에서 관측/미관측 상태를 분류하는 보조 시스템"이라는 표현을 유지한다 ("잔류자 확정", "대피 완료 확정"이라는 표현 금지).
- **False All-Clear(위험 상황인데 전원 안전으로 잘못 표시)를 가장 중요한 안전 지표로 취급한다.** 관리자 승인 없이 AI 단독으로 "전원 대피 완료"·"전원 안전"을 확정하는 화면/로직을 만들지 않는다.
- 데이터 수집 시 실제 연기 사용 금지(디지털 합성만), 외부 데이터는 라이선스·상업적 이용 가능 여부·출처 표기를 반드시 확인한다(CC BY 4.0 / CC BY-NC / MIT / Apache 등 라이선스 종류에 따라 활용 범위가 다름 — 애매하면 사용자에게 확인 요청).

---

## 3. 시스템 구성 (컴포넌트별 역할)

**비상시 대응** (화재 발생 이후 — 기존 파이프라인)

| 컴포넌트 | 역할 | 구현 방식 | BDAI 담당 여부 |
|---|---|---|---|
| 화재/연기 탐지 모델 | CCTV에서 불꽃·연기 감지 → 화재 의심 판정 | 객체 탐지(fire/smoke) 또는 VLM 프롬프트 | BDAI에서 라벨링·학습·평가 (공개 데이터셋 활용) |
| 경보 자동 트리거 | 화재/연기 탐지가 임계값 이상이면 자동으로 화재신호 발생 | 5초 주기로 탐지를 샘플링, **2회 연속** 임계값 이상이면 확정(`app/rules/alarm_trigger.py`) | VSCode(외부 규칙) — 관리자 수동 입력은 보조 오버라이드로 유지 |
| 작업자 탐지 모델 | CCTV에서 사람 탐지 | 객체 탐지 | BDAI에서 라벨링·학습·평가 |
| 안전모·조끼 탐지/분류 | 보호구·색상 분석 | 객체 탐지 또는 crop 분류. 카메라별로 헬멧/조끼 감지를 각각 켜고 끌 수 있음(`app/rules/ppe_settings.py`) | BDAI에서 라벨링·학습·평가 |
| 구역 판정 | 사람이 위치한 구역 판단 | 카메라별 폴리곤(점-폴리곤 판정, `app/rules/zone.py`) — 관리자가 카메라 화면에서 직접 그리고 이름 붙이는 편집 UI 있음(2026-08-04 신규) | VSCode(외부 규칙) |
| 매초 2차 확인 (화재 이후) | 화재경보가 뜬 순간부터 **매초** 그 프레임을 ZERO로 확인해 "체류중/쓰러진 사람/연기에 둘러싸인 사람" 카드를 하나씩 쌓음(개인별 추적 없음 — 2026-08-04 메모 참고) | ZERO가 찾은 우려 상황 박스를 그 순간 화면의 사람 박스와 **위치(중심점 거리)로 매칭**해 카테고리를 정확히 나눔(`probe_zone_situation`, `app/inference/situation_probe.py`) — 카테고리 합계는 항상 화면 인원수와 같음. 구역별 집계+사람별 박스를 함께 로그로 남김(`app/rules/zone_situation_log.py`) | ZERO 파운데이션 모델 |
| 화재·PPE 이벤트 로그 | 화재경보 발생 시각·구역, PPE 위반 발생·검토·정정 이력을 append-only로 기록 | 파일 기반 JSON 로그(`app/rules/fire_alert_log.py`/`ppe_violation_log.py`) | VSCode(외부 코드) |
| PPE 미착용 로그 | 화재 발생 여부와 무관하게 상시 실행. 추적을 안 쓰므로, 위반 감지 순간의 증거 사진(프레임+박스)만 위치·시간 기반으로 중복 없이 기록. 관리자가 카드를 열어 안전모/안전조끼 각각 착용·미착용으로 정정 가능(원판정은 감사 기록으로 유지), 위치 매칭 실패로 쪼개진 중복 노드는 타임라인에서 드래그로 병합 가능 | 같은 구역에서 최근 N초 이내 위치(IoU)·위반 조합이 같으면 "계속되는 같은 위반"으로 보고 건너뜀(`app/rules/ppe_violation_log.py`) | 없음 (규칙) |
| 구조 브리핑 챗봇 | 지금까지 기록된 화재경보·구역인원·2차 확인·PPE 위반 데이터(+시간 힌트가 있으면 그 시점 저장 프레임)를 근거로 자유 질문에 대화형으로 답함(2026-08-05, 예전 단발성 "요약 브리핑" 버튼 대체) | BDAI 테넌트에 이미 연결된 Gemini 게이트웨이(OpenAI SDK 호환, 기존 API 키 재사용) 호출(`app/inference/briefing.py`) — 시스템 프롬프트에 절대 원칙(전원 안전/대피완료 확정 금지, PPE는 "명" 아닌 "건"으로 답변 등)을 명시적으로 강제, 마크다운 후처리 안전망 포함 | Gemini (BDAI 게이트웨이 경유) |
| 프레임 선택 | 참조 이미지 선정 | 선명도·가림·박스 크기 점수 | VSCode(외부 코드) |
| 구조카드 생성 (개인별, 현재 미사용) | 구조정보 요약 — `app/api/briefing.py`에 API는 남아있으나, 개인별 추적을 걷어내며(2026-08-04) 프론트엔드에서 안 씀 | 구조화 데이터 템플릿 | 웹 서비스(VSCode) |
| ~~다중 객체 추적~~ / ~~상태 규칙 엔진~~ / ~~확인 우선순위 트리아지~~ / ~~사고 리플레이 화면~~ | 예전 개인별 추적 기반 설계(`WorkerEvent` 4상태)에서 쓰던 컴포넌트 | ByteTrack(`app/tracking/byte_track.py`)·`app/rules/state_engine.py`·`app/rules/triage.py`는 vestigial(단위 테스트만 유지, 실시간 파이프라인 미사용). 사고 리플레이(`app/api/incidents.py`)는 백엔드 API만 있고 프론트엔드 화면은 아직 없음 | — |

**평상시 안전관리** (2026-07-30 멘토링 피드백으로 추가된 축 — 작업자/PPE 탐지 모델을 화재 트리거 없이도 상시 실행)

| 컴포넌트 | 역할 | 구현 방식 | BDAI 담당 여부 |
|---|---|---|---|
| PPE 상시 점검 | 안전모·조끼 미착용 상시 감지 | 기존 작업자/PPE 탐지 모델 재사용, 화재경보와 무관하게 항상 실행 | 기존 PPE 모델 그대로 사용 |

> 멘토 피드백에서 언급된 위험행위 탐지(금지구역 흡연 등)·발화 위험 물질 탐지는 신규 클래스·데이터 확보 부담이 커서 **1차 구현 범위에서는 제외**하고 발표자료에 "확장 아이디어"로만 언급한다 (6번 MVP 범위 참고).

**BDAI와 VSCode는 같은 백엔드를 보는 두 창구다.** VSCode에서 Python SDK(`pip install superb-ai`, `Client(tenant=..., api_key=...)`)로 데이터 업로드·라벨 반입·학습 실행을 하면 BDAI 웹 화면에 즉시 반영된다. 별도 동기화 단계는 없다. API 키는 BDAI 테넌트 설정 → API 키 화면에서 발급.

---

## 4. 라벨 정의 (라벨링·코드 어디서든 이 정의를 기준으로 사용)

### 4-1. 객체 탐지 클래스

| 클래스 | 정의 | 포함 | 제외 |
|---|---|---|---|
| `person` | CCTV 화면에서 신체 일부/전체가 식별 가능한 작업자 | 머리·몸통 등으로 사람 판단 가능 | 사진, 포스터, 사람 모형, 반사 형상 |
| `helmet` | 머리에 착용한 산업용 안전모 | 안전모 외곽 식별 가능 | 일반 모자, 후드, 손에 든 안전모 |
| `vest` | 상체에 착용한 안전조끼 | 조끼 형태·반사띠 식별 | 일반 상의, 손에 든 조끼 |
| `fire` | 화면에서 식별 가능한 불꽃 | 실제 발화·화염 형상 | 붉은 조명, 화재 이미지가 담긴 포스터·화면 |
| `smoke` | 화면에서 식별 가능한 연기 | 연기 형태의 뿌연 영역 | 안개·수증기·역광에 의한 뿌연 화면 (구분 애매하면 `불명확` 속성으로) |

> ⚠️ **2026-07-31 추가 (PPE 데이터 색상 편향 대응)**: helmet/vest 신호의 ~93~95%를 차지하는 SFCHD 소스가 색상 자체도 편향돼 있어(vest 66.8%가 파랑, `docs/development_log.md` 33번 참고) "색만 보고 착용 여부를 판단"하는 얕은 상관관계를 학습할 위험이 실측으로 확인됐다. 색상 합성(hue jitter) augmentation과 별도로, **직접 미착용 신호를 학습시키는 보조 클래스**를 추가한다: `no_vest`(안전조끼 미착용 — 안전조끼 없이 일반 상의만), `head`(안전모 유무와 무관하게 식별 가능한 사람 머리 — head+helmet IoU 기반 착용 판정 규칙의 입력값). 새 데이터셋·자체 촬영 없이 CSS(`NO-Hardhat`/`NO-Safety Vest`)·SFCHD(`head`/`self_clothes`)에 이미 있던, 지금까지 반입하지 않았던 라벨을 그대로 재사용한다 (Task #5).
>
> ⚠️ **2026-07-31 정정 (`no_helmet` 클래스 제외)**: 위 추가 당시엔 `no_helmet`도 같이 넣었으나, 다양성 보강을 위해 SHWD(Safety Helmet Wearing Dataset)를 반입하는 과정에서 `no_helmet` 라벨이 69,227개로 다른 클래스 대비 압도적으로 쏠려 클래스 불균형을 만드는 게 확인돼 제외했다. `no_helmet`은 애초에 head+helmet IoU 규칙(`app/rules/ppe_compliance.py`)이 기하학적으로 미착용을 판정할 수 있어 없어도 helmet 판정 자체는 유지된다 — 다만 head가 아예 안 잡힌 경우엔 이제 미착용을 확정할 직접 신호가 없어 UNKNOWN으로 판단을 보류한다. `no_vest`는 조끼가 head 같은 "짝이 되는 신체 부위" 클래스를 가질 수 없어(기하학적 대안이 없음) 그대로 유지한다 (`docs/development_log.md` 참고).

`fire`/`smoke`는 **자체 촬영이 아니라 공개 데이터셋으로 학습**한다 (허가 없이 실제 불을 피울 수 없음). 자체 촬영 영상에 쓰는 "디지털 합성 연기"는 이 클래스 학습용이 아니라 `person`/`helmet`/`vest` 탐지가 저시야에서도 견디는지 테스트하는 용도로, 서로 목적이 다르다 (5번 Step 1 참고).

### 4-2. 속성 라벨

- 안전모 색상: 흰색·노란색·빨간색·파란색·기타·불명확
- 안전조끼 색상: 노란색·주황색·기타·불명확
- 상의 색상: 검정·흰색·회색·남색·파랑·빨강·기타·불명확
- 가시성: 높음·중간·낮음

### 4-3. 이벤트 라벨 (규칙 엔진 출력값 — 코드의 상태값 이름도 이걸 그대로 사용)

**작업자 상태(`WorkerEvent`, vestigial)**: `inside_observed`/`prolonged_presence`/`tracking_lost`/`camera_failure`. 2026-08-04 이후 실시간 파이프라인에서는 안 쓴다(2번 위 메모 참고) — 대신 아래 "구역별 상황 집계 카테고리"가 실제로 쓰이는 상태값이다.

**구역별 상황 집계 카테고리** (`app/inference/situation_probe.PROLONGED_PRESENCE_PROMPTS`, `ZoneSituationEntry.breakdown`의 key): `체류중`(우려 신호 없이 관측만 됨), `쓰러진 사람`, `연기에 둘러싸인 사람` — **화재경보 후 매초** 집계한다(2026-08-04 이후 — 예전엔 "장기체류 전환 시에만"이었으나 개인별 장기체류 개념 자체가 없어졌다).

### 4-4. 애매한 사례 처리 기준

| 상황 | 처리 |
|---|---|
| 조명 때문에 색상 불명확 | `불명확` |
| 사람이 화면 가장자리로 나가거나 화재 중 화면에서 사라짐 | 안전/위험 어느 쪽으로도 확정하지 않음 — 그 순간 이후 2차 확인 카드에 그냥 안 잡힐 뿐, 별도 "관측 끊김" 상태값을 표시하지 않는다 |
| 화재경보 후 2차 확인에서 우려 신호 없음 | "체류중"으로 집계, 경고 아님 |
| 화재경보 후 2차 확인에서 "쓰러진 사람"/"연기에 둘러싸인 사람" 감지 | 해당 카테고리로 집계하고 카드·타임라인에 강조 표시(확인 필요 신호일 뿐, 구조 필요 확정 아님) |
| 두 사람이 동일 복장 | 복장만으로 동일인 병합 금지 |
| 카메라 영상 중단 | 마지막으로 확보된 프레임 이후로는 2차 확인 카드가 더 안 쌓일 뿐, 별도 확정 상태값 없음 |

---

## 5. 작업 파이프라인 (해커톤 4단계 = BDAI 화면 절차)

해커톤 규정: 4단계 파이프라인(데이터 선별 → 데이터 구축/라벨링 → 모델 학습 → 서비스 배포)을 Superb AI 플랫폼 안에서 수행하며, 각 단계의 플랫폼 로그가 심사 근거로 쓰인다. **모델 학습은 플랫폼 밖(로컬/Colab 등)에서 해도 감점 없음** — 다만 데이터 관리·라벨링·자동 라벨링·배포 등 플랫폼 기능을 일부라도 쓰면 가점. ZERO(파운데이션 모델)·VLM을 그대로 활용하는 것도 허용된다.

### Step 1. 데이터 선별 — **A 담당**
- **자체 촬영 (person/helmet/vest + 추적·관측지속시간 판정 테스트용)**: 강의실·복도·실습실에서 카메라 2대로 촬영 (A·B·C 3구역, 인원 2~4명 교대). 시나리오 10종 이상 (정상 대피, 내부 잔류, 가림에 의한 추적 중단, 집단 대피, 동일 복장 교차, 화재경보 후 장기 체류, 디지털 연기, 카메라 중단, 화면 이탈). 목표: 원본 영상 40~60개(20~45초), 샘플링 이미지 2,000~3,000장. 여기서 "디지털 연기"는 fire/smoke 클래스 학습용이 아니라 저시야 상황에서 person/helmet/vest 탐지가 견디는지 테스트하는 용도.
- **공개 데이터셋 (fire/smoke 클래스 + person/helmet/vest 보강용)**: COCO(person), Kaggle/Roboflow의 Hard Hat/Safety Vest 데이터셋, 공개 fire·smoke 탐지 데이터셋 등에서 확보. 반드시 라이선스(CC BY 4.0 / CC BY-NC / MIT / Apache 등)·상업적 이용 가능 여부·출처 표기 의무를 확인 후 사용 (애매하면 사용자에게 확인).
- 자체 촬영분·공개 데이터셋 모두 BDAI 데이터셋으로 업로드 (원본 이미지만 업로드하는 것은 항상 가능; 공개 데이터셋에 이미 라벨이 있는 경우 SDK로 그대로 반입할 수 있는지는 `docs.bdai.superb-ai.com` SDK 문서에서 별도 확인 필요 — 안 되면 BDAI 자동 라벨링/ZERO 모델로 pre-fill 후 검수하는 방식으로 대체)

### Step 2. 데이터 구축(라벨링) — **A 담당**
- BDAI에서 프로젝트 생성 → 스키마에 4-1 클래스(person/helmet/vest/fire/smoke)와 4-2 속성 등록. person/helmet/vest와 fire/smoke는 목적이 다르므로 **별도 프로젝트로 분리**하는 것을 권장 (데이터 출처·라벨링 담당·스냅샷 주기가 다름)
- 사람·안전모·조끼 라벨링, 목표: 사람 박스 6,000개 이상, PPE 라벨 3,000개 이상
- fire/smoke는 공개 데이터셋 기준으로 목표 수량 별도 산정 (다운로드한 데이터셋 규모에 맞춰 조정)
- 검수(리뷰) 진행 후 스냅샷 생성 → 학습은 항상 스냅샷 기준

### Step 3. 모델 학습(or ZERO 모델 사용) — **A·B 공동**
- 실험 로그를 순서대로 기록 (기획서 12번 섹션 실험 계획 10차 순서를 그대로 따름): 베이스라인 → 가림/원거리 추가 → 저조도/연기 증강 → PPE 추가 → 클래스 불균형 보완 → 오류 데이터 재수집 → 추적 연결 → 관측지속시간 규칙 적용 → 동일 복장/집단 시험 → 임계값 조정 — ⚠️ 마지막 3단계 중 "추적 연결"·"관측지속시간 규칙 적용"은 2026-08-04 파이프라인 전환으로 실시간 서비스에서는 더 이상 안 쓴다(실험 이력으로만 남김, `experiments/logs/`)
- 실험마다 mAP, Precision, Recall, F1, IDF1, ID Switch, False All-Clear Rate 중 해당 지표 기록
- 학습은 BDAI 플랫폼에서 해도 되고, 로컬/Colab 등 외부에서 해도 됨. 외부에서 했다면 최소한 데이터 관리·라벨링은 BDAI에서 했다는 흔적(로그)을 남길 것

### Step 4. 서비스 배포 및 데모 — **B 담당**
- 추론 결과 → 화재경보 자동 트리거 → **화재경보 후 매초 2차 확인**(ZERO) → 통합 타임라인(평상시 PPE 이력 + 화재 발생 + 2차 확인 이정표) + 구조 브리핑 챗봇(Gemini)
- 최종 발표에는 반드시 **실제 동작하는 데모**가 있어야 함 — Render에 Docker로 배포한 외부 접속 가능한 데모 있음(`Dockerfile`/`render.yaml`, `docs/development_log.md` 58번)
- 대시보드는 **평상시 모드**(PPE 준수율, 구역별 인원)와 **비상시 모드**(현재 관측 인원, 구역별 인원, 화재경보 배너)를 자동 전환하며 한 화면 안에 공존한다. 화재 모드에서는 감지된 사람 전원을 초록 박스로만 표시하고(개인별 상태 구분 없음), 매초 2차 확인 카드가 별도 그리드에 쌓인다. PPE 위반·2차 확인 카드에는 항상 "추정 정보 — 확정 아님"을 노출해 정보가 추정치임을 드러낸다. 화면 구성 상세는 `docs/screen_guide.md`(2026-08-04 실제 구현 기준으로 재작성됨) 참고.

---

## 6. MVP 범위 (스코프 크립 방지용 — 이 목록 밖의 기능은 우선순위 낮음)

**필수 — 비상시 대응** (2026-08-04 재설계 반영): 고정 카메라 입력, **화재/연기 자동 탐지 + 경보 자동 트리거**(5초 주기 샘플링, 2회 연속 감지 시 확정), **화재경보 발생 시점부터 매초** 그 프레임을 2차 확인(체류중/쓰러진 사람/연기에 둘러싸인 사람 인원수), 감지된 사람 전원 실시간 박스 표시(개인별 상태 구분 없음), 구역 매핑(관리자가 폴리곤을 직접 그리고 이름 붙이는 편집 UI), 평상시 PPE 이력 + 화재 발생 + 2차 확인을 잇는 통합 타임라인, 지금까지 기록(+시간 힌트가 있으면 그 시점 프레임)을 근거로 답하는 Gemini 구조 브리핑 챗봇, 화재경보·PPE 상태 변화 타임스탬프 로그, 외부 접속 가능한 배포(Render)

**필수 — 평상시 안전관리** (2026-07-30 추가, 2026-08-02·2026-08-04 재설계): PPE(안전모·조끼, 각각 on/off 토글 가능) 상시 미착용 감지 — **추적을 쓰지 않고** person 박스마다 그때그때 판정, 위반은 위치+시간 기반으로 중복 없이 증거 사진과 함께 기록, 관리자가 카드를 열어 직접 정정 가능(원판정은 감사 기록으로 유지), 위치 매칭 실패로 쪼개진 중복 노드는 드래그로 병합. 평상시/비상시 모드 전환(화재경보 발생 시 자동 — 이 시점부터 매초 2차 확인이 시작되고 PPE 판정은 멈춘다)

**구현했지만 백엔드 API만 있고 프론트엔드 화면은 없는 것**: 사고 리플레이(화재경보/PPE 로그 병합 타임라인). 원래 MVP 필수 범위였으나 시간 관계상 화면 연결은 아직 안 함 — 백엔드 로직·테스트는 그대로 있으므로 시간이 되면 화면만 붙이면 된다.

**확장 아이디어 (시간 남으면, 발표자료에는 방향성만 언급 가능)**: 위험행위 탐지(금지구역 흡연, 발화 위험 작업), 발화 위험 물질/위험 요소 방치 탐지 — 신규 탐지 클래스와 별도 데이터 확보가 필요해 1차 구현 범위에서는 제외

**제외 (만들지 않음)**: 119 자동 연동, 얼굴인식/사번 매칭, 다중 카메라 Re-ID, 체형·성별·나이·국적 추정, 화재 확산 시뮬레이션, 소방대 수색 경로 자동 명령, AI 단독 전원 대피 선언, 소화기/가연물 제거 행동 명령

---

## 7. 해커톤 규정 체크리스트

- [ ] 트랙(산업안전·물류) 명시, 시나리오는 예시일 뿐이므로 자체 재정의 내용을 문서화 (재정의는 가점 요소)
- [ ] 외부 데이터 사용 시 라이선스 종류·상업적 이용 가능 여부·출처 표기 확인 (애매하면 사용자에게 확인)
- [ ] 각 파이프라인 단계에서 BDAI 플랫폼 로그가 남도록 진행 (심사 근거)
- [ ] 최종 제출 보고서 구조(기획서 22번 섹션) 준수: 참가자 정보, 클래스 정의서, 모델 실험 로그, 최종 모델 성능, 서비스 구현(데모 링크), 기대효과·한계
- [ ] 발표 구성(기획서 23번 섹션): 문제 → 해결책 → 안전 원칙 → 차별점 순서
- ⚠️ 과제 정의서 6~13페이지(심사 기준·제출 방법·일정 등 가능성)를 아직 확보하지 못함. 관련 내용이 필요해지면 사용자에게 원본 확인 요청할 것

---

## 8. 작업 시 커뮤니케이션 규칙

- 이 폴더에서의 진행 상황 설명, 요약, 결과 보고는 한국어로 작성한다 (전역 지침).
- 클래스명·이벤트 라벨명 등 코드 식별자는 위 4번 표에 정의된 영문 스네이크케이스(`person`, `not_worn`, `abnormal` 등)를 그대로 사용하고 임의로 바꾸지 않는다.
- 안전/개인정보 원칙(2번 섹션)과 충돌할 수 있는 기능 요청을 받으면, 구현 전에 반드시 사용자에게 확인한다.
