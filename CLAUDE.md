# LastSight AI — 프로젝트 지침 (SuperB AI × BDAI 해커톤)

이 파일은 이 프로젝트 폴더에서 작업할 때 항상 따라야 할 규칙과 맥락을 담고 있다.
세부 배경 자료는 아래 파일을 참고한다 (필요할 때만 열어서 확인, 매번 전체를 다시 읽지 않아도 됨):

- `docs/LastSight_AI_최종기획서.html` — 프로젝트 전체 기획서 (문제 정의, 시나리오, 클래스 정의, 지표, MVP 범위, 발표 구성 등 25개 섹션)
- `docs/Superb_AI_X_BDAI_해커톤_과제_정의.pdf` — 해커톤 공식 과제 정의 (1~5p 확보, 6~13p는 미확보 상태 — 심사 기준·제출 방법·일정이 나올 경우 사용자에게 추가로 확인)
- `docs/bdai-platform-guide-study.md` — BDAI 플랫폼 사용 매뉴얼 전체 39페이지 요약본 (화면별 기능, 용어, SDK 사용법)
- `docs/bdai-platform-guide-beginner.md` — 위 매뉴얼의 완전 초보자용 버전
- `docs/development_log.md` — 개발 과정 기록 (발표자료·질의응답 대비용). **의미 있는 작업 단계(결정·구현·문제해결)가 끝날 때마다 이 파일에 이어서 기록할 것** — 무엇을 했는지, 왜 그렇게 했는지, 막힌 부분은 어떻게 풀었는지 순서대로

프로젝트 폴더 구조와 각 디렉터리의 역할은 `README.md`를 참고한다.

---

## 1. 프로젝트 한 줄 요약

**LastSight AI**: RGB CCTV를 활용해 **평상시에는 산업현장의 화재·안전사고를 예방**(PPE 착용 확인, 소화기·전기패널·비상구 앞 관리구역 상시 감시)하고, **비상시에는 연기·불꽃을 감지해 화재경보를 자동 트리거한 뒤 화재경보 발생 이후 작업자가 얼마나 오래 관측되고 있는지를 추적**해 "누가 지금도 관측되고 있고, 얼마나 오래 관측 중이며, 관측이 끊긴 사람은 마지막으로 어디서 봤는지"를 구조 브리핑 카드로 만들어주는 통합 산업안전 비전 시스템.

> ⚠️ **2026-07-28 방향 전환**: 원래 `docs/LastSight_AI_최종기획서.html`은 "화재를 발견하는 AI가 아니라"를 핵심 차별점으로 내세우며 화재신호를 관리자가 수동 입력하는 것으로 설계했었다. 이후 논의를 거쳐 **화재/연기 자동 탐지를 시스템에 포함**시키는 것으로 방향을 바꿨다. 차별점은 "화재 탐지에서 그치지 않고, 그 이후 대피 이행까지 끝까지 확인하는 AI"로 재정의한다 (화재 탐지 자체보다 그 이후 정보 공백 해소가 여전히 핵심 가치). **`docs/LastSight_AI_최종기획서.html` 원문은 아직 이 변경을 반영하지 못했으니, 그 문서의 "화재를 발견하는 AI가 아니라" 관련 서술·2단계 화재신호 입력 부분을 읽을 땐 이 CLAUDE.md 쪽이 최신 기준임을 우선한다.** 발표자료·제출보고서 작성 시점에는 기획서 원문도 이 방향에 맞게 함께 수정해야 한다.
>
> ⚠️ **2026-07-30 방향 확장 (Superb AI 멘토링 피드백 반영, `docs/Superb_AI_공모전_멘토링_QnA.md` 참고)**: "화재 이후 잔류인원 추적"에만 집중하면 데이터 부족·인식 오류로 구현 난도가 지나치게 높아진다는 멘토 피드백에 따라, **평상시 예방 기능을 프로젝트의 정식 축으로 확장**한다. 기존 "비상시 대응"(화재 탐지→추적→상태 판정→구조카드) 축은 그대로 유지하되, 그 앞단에 "평상시 안전관리"(PPE 상시 점검, 관리구역 상시 감시) 축을 더해 두 축으로 프로젝트를 구성한다. 잔류인원 관련 정보는 여전히 "확정된 사실"이 아니라 **보조 정보로만 제공**한다는 원칙은 그대로 유지 — 오히려 이번 피드백으로 이 원칙이 왜 중요한지가 재확인됐다.
>
> ⚠️ **2026-07-30 설계 단순화 (소화기 확인 방식 변경)**: 위 예방 축 확장 당시에는 소화기 확인을 위해 `fire_extinguisher` 신규 탐지 클래스를 학습할 계획이었으나, 실제 CCTV는 각도·거리·조명이 제각각이라 공개 데이터셋(대부분 정면·근접 촬영)으로 학습한 탐지기가 잘 맞지 않을 위험이 컸다. 대신 소화기·전기패널·비상구 앞 공간을 모두 **"관리구역(clearance zone)"**으로 등록하고, 기준 사진 대비 SSIM 기반 변화 감지로 이상 여부를 판단하는 단일 메커니즘으로 통합했다 (신규 데이터셋·학습 불필요, `docs/development_log.md` 16번 참고). 이에 따라 `fire_extinguisher`는 객체 탐지 클래스 목록(4-1)에서 제외한다.
>
> ⚠️ **2026-07-30 핵심 파이프라인 전환 (출구 통과 판정 폐기 → 관측 지속시간 모델)**: 기존 "출구 가상선 통과 판정" 방식은 카메라 시야가 출구 바깥까지 잡지 못하는 경우(실제 현장에서 흔함) "통과 확인" 자체가 구조적으로 영원히 성립할 수 없다는 한계가 드러났다 (`docs/development_log.md` 17번 참고). 이에 따라 출구 통과·대피 완료 판정 기능을 전면 폐기하고, **화재경보 발생 이후 각 작업자가 지금도 관측되고 있는지·얼마나 오래 관측되고 있는지**만 보는 방식으로 대체했다 (`docs/development_log.md` 18번 참고). 상태값은 `inside_observed`/`prolonged_presence`/`tracking_lost`/`camera_failure` 4가지이며, 화재경보 후 기본 5분(`PROLONGED_PRESENCE_MINUTES`)을 넘도록 관측되면 `prolonged_presence`로 전환해 대시보드에 강조 표시한다. 관측이 끊긴 경우(`tracking_lost`)는 여전히 안전도 위험도 확정하지 않는다 — 세부 반영 내용은 2번(절대 원칙), 3번(시스템 구성), 4-3번(이벤트 라벨), 6번(MVP 범위) 참고.

- 트랙: 산업안전·물류
- 팀 구성: 2인 — **A. 데이터/라벨링 담당**(BDAI 플랫폼 중심), **B. 모델/서비스 개발 담당**(VSCode 중심). 실제로는 두 역할이 계속 맞물리므로 아래 파이프라인 단계마다 누가 주도하는지 표시해 둠.

---

## 2. 절대 원칙 (코드·문서·발표 어디서든 위반 금지)

기획서에 명시된 안전·개인정보 설계는 이 프로젝트의 핵심 차별점이자 심사 포인트다. 기능 구현, 텍스트 작성(UI 문구, 로그, 발표자료 포함) 어디에서도 아래를 어겨선 안 된다.

- **관측이 끊겼다고 "대피 완료"로, 계속 관측된다고 "위험 확정"으로 간주하지 않는다.** 이 시스템은 출구 통과·대피 완료를 확정 선언하는 기능을 두지 않는다. 화재경보 발생 시점부터 각 작업자가 지금도 관측되고 있는지·얼마나 오래 관측되고 있는지만 보며, 관측이 끊긴 경우(`tracking_lost`)는 안전/위험 어느 쪽으로도 해석하지 않고 마지막 관측 위치·시각만 보조 정보로 제공한다.
- **화재경보 후 일정 시간(기본 5분, `PROLONGED_PRESENCE_MINUTES`)을 넘도록 관측되는 작업자는 대시보드에서 강조 표시한다(`prolonged_presence`).** 다만 이 표시도 "구조 필요 확정"이 아니라 "확인이 필요하다"는 신호일 뿐이며 관리자 판단을 대체하지 않는다.
- **얼굴인식·사번 매칭·신원 특정 금지**. 국적·인종·성별·나이·체형 추정 로직이나 UI 문구를 절대 추가하지 않는다. 사람은 임시 추적 ID로만 다룬다.
- **동일 복장 = 동일인이 아니다**. 복장(색상 등)만으로 두 사람을 같은 ID로 병합하는 로직을 넣지 않는다.
- **공장 전체 재실 인원을 확정하지 않는다.** "CCTV 관측 범위 내에서 관측/미관측 상태를 분류하는 보조 시스템"이라는 표현을 유지한다 ("잔류자 확정", "대피 완료 확정"이라는 표현 금지).
- **False All-Clear(위험 상황인데 전원 안전으로 잘못 표시)를 가장 중요한 안전 지표로 취급한다.** 관리자 승인 없이 AI 단독으로 "전원 대피 완료"·"전원 안전"을 확정하는 화면/로직을 만들지 않는다.
- 데이터 수집 시 실제 연기 사용 금지(디지털 합성만), 외부 데이터는 라이선스·상업적 이용 가능 여부·출처 표기를 반드시 확인한다(CC BY 4.0 / CC BY-NC / MIT / Apache 등 라이선스 종류에 따라 활용 범위가 다름 — 애매하면 사용자에게 확인 요청).

---

## 3. 시스템 구성 (컴포넌트별 역할)

**비상시 대응** (화재 발생 이후 — 기존 파이프라인)

| 컴포넌트 | 역할 | 구현 방식 | BDAI 담당 여부 |
|---|---|---|---|
| 화재/연기 탐지 모델 | CCTV에서 불꽃·연기 감지 → 화재 의심 판정 | 객체 탐지(fire/smoke) 또는 VLM 프롬프트 | BDAI에서 라벨링·학습·평가 (공개 데이터셋 활용) |
| 경보 자동 트리거 | 화재/연기 탐지가 임계값 이상이면 자동으로 화재신호 발생 | 탐지 결과 기반 규칙 | VSCode(외부 규칙) — 관리자 수동 입력은 보조 오버라이드로 유지 |
| 작업자 탐지 모델 | CCTV에서 사람 탐지 | 객체 탐지 | BDAI에서 라벨링·학습·평가 |
| 안전모·조끼 탐지/분류 | 보호구·색상 분석 | 객체 탐지 또는 crop 분류 | BDAI에서 라벨링·학습·평가 |
| 다중 객체 추적 | 사람별 임시 ID 유지 | ByteTrack 계열 | VSCode(외부 코드)에서 모델 결과와 결합 |
| 구역 판정 | 사람이 위치한 구역 판단 | 카메라별 고정 폴리곤 | VSCode(외부 규칙) |
| 상태 규칙 엔진 | 화재경보 후 관측 지속 여부·시간 판정(관측중/장기체류경고/관측안됨/카메라확인불가) | 추적 결과 + 경과시간 기반 규칙 | VSCode(외부 코드) |
| 화재·작업자 이벤트 로그 | 화재경보 발생 시각·구역, 작업자 상태 전환 시각을 append-only로 기록 | 파일 기반 JSON 로그 | VSCode(외부 코드) |
| 프레임 선택 | 참조 이미지 선정 | 선명도·가림·박스 크기 점수 | VSCode(외부 코드) |
| 구조카드 생성 | 구조정보 요약 | 구조화 데이터 템플릿 | 웹 서비스(VSCode) |

**평상시 안전관리** (2026-07-30 멘토링 피드백으로 추가된 축 — 작업자/PPE 탐지 모델을 화재 트리거 없이도 상시 실행)

| 컴포넌트 | 역할 | 구현 방식 | BDAI 담당 여부 |
|---|---|---|---|
| PPE 상시 점검 | 안전모·조끼 미착용 상시 감지 | 기존 작업자/PPE 탐지 모델 재사용, 화재경보와 무관하게 항상 실행 | 기존 PPE 모델 그대로 사용 |
| 관리구역(소화기/전기패널/비상구) 상시 감시 | 등록된 구역의 기준 사진과 현재 화면을 비교해 지속적 변화(가려짐·치워짐·물체 방치)를 감지 | SSIM 기반 변화 감지, 사람과 겹치는 구간은 평가 제외, 변화가 15분 이상 지속되면 `abnormal` — 신규 탐지 클래스·학습 불필요 | 없음 (VSCode 자체 알고리즘, 기준 사진 등록만 API로 처리) |

> 멘토 피드백에서 언급된 위험행위 탐지(금지구역 흡연 등)·발화 위험 물질 탐지는 신규 클래스·데이터 확보 부담이 커서 **1차 구현 범위에서는 제외**하고 발표자료에 "확장 아이디어"로만 언급한다 (6번 MVP 범위 참고).

**BDAI와 VSCode는 같은 백엔드를 보는 두 창구다.** VSCode에서 Python SDK(`pip install superb-ai`, `Client(tenant=..., api_key=...)`)로 데이터 업로드·라벨 반입·학습 실행을 하면 BDAI 웹 화면에 즉시 반영된다. 별도 동기화 단계는 없다. API 키는 BDAI 테넌트 설정 → API 키 화면에서 발급.

---

## 4. 라벨 정의 (라벨링·코드 어디서든 이 정의를 기준으로 사용)

### 4-1. 객체 탐지 클래스

| 클래스 | 정의 | 포함 | 제외 |
|---|---|---|---|
| `person` | CCTV 화면에서 신체 일부/전체가 식별 가능한 작업자 | 머리·몸통 등으로 사람 판단 가능 | 사진, 포스터, 사람 모형, 반사 형상 |
| `helmet` | 머리에 착용한 산업용 안전모 | 안전모 외곽 식별 가능 | 일반 모자, 후드, 손에 든 안전모 |
| `vest` | 상체에 착용한 안전조끼 | 조끼 형태·반사띠 식별 | 일반 상의, 손에 든 조끼 |
| `fire` | 화면에서 식별 가능한 불꽃 | 실제 발화·화염 형상 | 붉은 조명, 화재 이미지가 담긴 포스터·화면 |
| `smoke` | 화면에서 식별 가능한 연기 | 연기 형태의 뿌연 영역 | 안개·수증기·역광에 의한 뿌연 화면 (구분 애매하면 `불명확` 속성으로) |

> 소화기·전기패널·비상구는 별도 탐지 클래스로 학습하지 않는다 — "관리구역" 폴리곤 등록 + 기준 사진 대비 변화 감지로 처리한다 (1번 방향 정정 메모, 3번 참고).

`fire`/`smoke`는 **자체 촬영이 아니라 공개 데이터셋으로 학습**한다 (허가 없이 실제 불을 피울 수 없음). 자체 촬영 영상에 쓰는 "디지털 합성 연기"는 이 클래스 학습용이 아니라 `person`/`helmet`/`vest` 탐지가 저시야에서도 견디는지 테스트하는 용도로, 서로 목적이 다르다 (5번 Step 1 참고).

### 4-2. 속성 라벨

- 안전모 색상: 흰색·노란색·빨간색·파란색·기타·불명확
- 안전조끼 색상: 노란색·주황색·기타·불명확
- 상의 색상: 검정·흰색·회색·남색·파랑·빨강·기타·불명확
- 가시성: 높음·중간·낮음

### 4-3. 이벤트 라벨 (규칙 엔진 출력값 — 코드의 상태값 이름도 이걸 그대로 사용)

**작업자 상태(`WorkerEvent`)**: `inside_observed`(현재 관측 중), `prolonged_presence`(화재경보 후 임계시간 넘도록 관측 지속 — 확인 필요), `tracking_lost`(현재 관측 안 됨 — 안전/위험 미확정), `camera_failure`(카메라 확인 불가)

**관리구역 상태(`ClearanceZoneState`)**: `normal`(기준 사진과 일치), `observing`(변화 감지됐으나 지속시간 미달), `abnormal`(15분 이상 변화 지속 — 확인 필요), `camera_failure`(카메라 확인 불가)

### 4-4. 애매한 사례 처리 기준

| 상황 | 처리 |
|---|---|
| 조명 때문에 색상 불명확 | `불명확` |
| 사람이 화면 가장자리로 나감 | `tracking_lost` (대피 여부 판단 안 함) |
| 화재경보 후 임계시간 이내 계속 관측됨 | `inside_observed` 유지, 경고 아님 |
| 화재경보 후 임계시간 초과 관측됨 | `prolonged_presence`로 전환, 대시보드 강조 표시 |
| 관리구역 앞을 사람이 잠깐 가로막음 | 사람과 겹치는 구간은 평가에서 제외, 직전 상태 유지 |
| 관리구역 변화가 짧게 나타났다 사라짐 | 지속 15분 미만이면 `normal` 유지 (이상 아님) |
| 두 사람이 동일 복장 | 복장만으로 동일인 병합 금지 |
| 카메라 영상 중단 | 중단 직전 마지막 위치 유지, `camera_failure` |

---

## 5. 작업 파이프라인 (해커톤 4단계 = BDAI 화면 절차)

해커톤 규정: 4단계 파이프라인(데이터 선별 → 데이터 구축/라벨링 → 모델 학습 → 서비스 배포)을 Superb AI 플랫폼 안에서 수행하며, 각 단계의 플랫폼 로그가 심사 근거로 쓰인다. **모델 학습은 플랫폼 밖(로컬/Colab 등)에서 해도 감점 없음** — 다만 데이터 관리·라벨링·자동 라벨링·배포 등 플랫폼 기능을 일부라도 쓰면 가점. ZERO(파운데이션 모델)·VLM을 그대로 활용하는 것도 허용된다.

### Step 1. 데이터 선별 — **A 담당**
- **자체 촬영 (person/helmet/vest + 추적·관측지속시간 판정 테스트용)**: 강의실·복도·실습실에서 카메라 2대로 촬영 (A·B·C 3구역, 인원 2~4명 교대). 시나리오 10종 이상 (정상 대피, 내부 잔류, 가림에 의한 추적 중단, 집단 대피, 동일 복장 교차, 화재경보 후 장기 체류, 디지털 연기, 카메라 중단, 화면 이탈, 관리구역 앞 물체 방치/제거). 목표: 원본 영상 40~60개(20~45초), 샘플링 이미지 2,000~3,000장. 여기서 "디지털 연기"는 fire/smoke 클래스 학습용이 아니라 저시야 상황에서 person/helmet/vest 탐지가 견디는지 테스트하는 용도.
- **공개 데이터셋 (fire/smoke 클래스 + person/helmet/vest 보강용)**: COCO(person), Kaggle/Roboflow의 Hard Hat/Safety Vest 데이터셋, 공개 fire·smoke 탐지 데이터셋 등에서 확보. 반드시 라이선스(CC BY 4.0 / CC BY-NC / MIT / Apache 등)·상업적 이용 가능 여부·출처 표기 의무를 확인 후 사용 (애매하면 사용자에게 확인).
- 자체 촬영분·공개 데이터셋 모두 BDAI 데이터셋으로 업로드 (원본 이미지만 업로드하는 것은 항상 가능; 공개 데이터셋에 이미 라벨이 있는 경우 SDK로 그대로 반입할 수 있는지는 `docs.bdai.superb-ai.com` SDK 문서에서 별도 확인 필요 — 안 되면 BDAI 자동 라벨링/ZERO 모델로 pre-fill 후 검수하는 방식으로 대체)

### Step 2. 데이터 구축(라벨링) — **A 담당**
- BDAI에서 프로젝트 생성 → 스키마에 4-1 클래스(person/helmet/vest/fire/smoke)와 4-2 속성 등록. person/helmet/vest와 fire/smoke는 목적이 다르므로 **별도 프로젝트로 분리**하는 것을 권장 (데이터 출처·라벨링 담당·스냅샷 주기가 다름)
- 사람·안전모·조끼 라벨링, 목표: 사람 박스 6,000개 이상, PPE 라벨 3,000개 이상
- fire/smoke는 공개 데이터셋 기준으로 목표 수량 별도 산정 (다운로드한 데이터셋 규모에 맞춰 조정)
- 검수(리뷰) 진행 후 스냅샷 생성 → 학습은 항상 스냅샷 기준

### Step 3. 모델 학습(or ZERO 모델 사용) — **A·B 공동**
- 실험 로그를 순서대로 기록 (기획서 12번 섹션 실험 계획 10차 순서를 그대로 따름): 베이스라인 → 가림/원거리 추가 → 저조도/연기 증강 → PPE 추가 → 클래스 불균형 보완 → 오류 데이터 재수집 → 추적 연결 → 관측지속시간 규칙 적용 → 동일 복장/집단 시험 → 임계값 조정
- 실험마다 mAP, Precision, Recall, F1, IDF1, ID Switch, False All-Clear Rate 중 해당 지표 기록
- 학습은 BDAI 플랫폼에서 해도 되고, 로컬/Colab 등 외부에서 해도 됨. 외부에서 했다면 최소한 데이터 관리·라벨링은 BDAI에서 했다는 흔적(로그)을 남길 것

### Step 4. 서비스 배포 및 데모 — **B 담당**
- 추론 결과 → ByteTrack 연동 → 화재경보 후 관측 지속시간 판정 → 상태 규칙 엔진 → 구조카드/대시보드
- 최종 발표에는 반드시 **실제 동작하는 데모**가 있어야 함 (웹앱/API 서버/영상 추론 시연 등 형식 자유)
- 대시보드는 **평상시 모드**(PPE 준수율, 관리구역 상태, 구역별 인원)와 **비상시 모드**(상단 요약: 관측 대상/관측 중/장기체류경고/관측안됨 인원, 평면도, 작업자 타임라인, 구조카드)로 구성하고 화재경보 발생 시 자동 전환한다. 장기체류경고·관측안됨 작업자 카드에는 마지막 확인 시각·위치뿐 아니라 **탐지 신뢰도·시야 확보 여부**도 함께 표시해 정보가 추정치임을 드러낸다. 화면 구성 상세는 UI/UX 담당자용으로 별도 정리한 화면 가이드(`docs/screen_guide.md`, 대시보드 5개 화면 뼈대) 참고.

---

## 6. MVP 범위 (스코프 크립 방지용 — 이 목록 밖의 기능은 우선순위 낮음)

**필수 — 비상시 대응**: 고정 카메라 입력, **화재/연기 자동 탐지 + 경보 자동 트리거**, 작업자 탐지+단일 카메라 내 추적, 구역 매핑, 화재경보 후 관측 지속 여부 기반 4가지 상태(관측중/장기체류경고/관측안됨/카메라확인불가) 판정, 마지막 관측 구역·시각·이미지, 참조 프레임 선택, 안전모·조끼·상의 분석, 구조 브리핑 카드+대시보드, 다국어 대피 문구, 화재경보·작업자 상태 변화 타임스탬프 로그

**필수 — 평상시 안전관리** (2026-07-30 추가): PPE(안전모·조끼) 상시 미착용 감지, 관리구역(소화기/전기패널/비상구) 등록+기준사진 대비 변화 감지(15분 이상 지속 시 경고), 평상시/비상시 모드 전환(화재경보 발생 시 자동)

**확장 아이디어 (시간 남으면, 발표자료에는 방향성만 언급 가능)**: 위험행위 탐지(금지구역 흡연, 발화 위험 작업), 발화 위험 물질/위험 요소 방치 탐지 — 신규 탐지 클래스와 별도 데이터 확보가 필요해 1차 구현 범위에서는 제외

**제외 (만들지 않음)**: 119 자동 연동, 얼굴인식/사번 매칭, 다중 카메라 Re-ID, 체형·성별·나이·국적 추정, 화재 확산 시뮬레이션, 소방대 수색 경로 자동 명령, AI 단독 전원 대피 선언, 소화기/가연물 제거 행동 명령

---

## 7. 해커톤 규정 체크리스트

- [ ] 트랙(산업안전·물류) 명시, 시나리오는 예시일 뿐이므로 자체 재정의 내용을 문서화 (재정의는 가점 요소)
- [ ] 외부 데이터 사용 시 라이선스 종류·상업적 이용 가능 여부·출처 표기 확인 (애매하면 사용자에게 확인)
- [ ] 각 파이프라인 단계에서 BDAI 플랫폼 로그가 남도록 진행 (심사 근거)
- [ ] 최종 제출 보고서 구조(기획서 22번 섹션) 준수: 참가자 정보, 클래스 정의서, 모델 실험 로그, 최종 모델 성능, 서비스 구현(데모 링크), 기대효과·한계
- [ ] 발표 구성(기획서 23번 섹션): 문제 → 해결책 → 안전 원칙 → 차별점 순서
- ⚠️ 과제 정의서 6~13페이지(심사 기준·제출 방법·일정 등 가능성)를 아직 확보하지 못함. 관련 내용이 필요해지면 사용자에게 원본 확인 요청할 것

---

## 8. 작업 시 커뮤니케이션 규칙

- 이 폴더에서의 진행 상황 설명, 요약, 결과 보고는 한국어로 작성한다 (전역 지침).
- 클래스명·이벤트 라벨명 등 코드 식별자는 위 4번 표에 정의된 영문 스네이크케이스(`person`, `inside_observed` 등)를 그대로 사용하고 임의로 바꾸지 않는다.
- 안전/개인정보 원칙(2번 섹션)과 충돌할 수 있는 기능 요청을 받으면, 구현 전에 반드시 사용자에게 확인한다.
