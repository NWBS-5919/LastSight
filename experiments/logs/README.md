# 실험 로그

`docs/LastSight_AI_최종기획서.html` 12번 섹션(모델 실험 계획) 1~10차 순서를 따라 실험마다 파일을 하나씩 추가한다.
파일명 규칙: `NN-실험명.md` (예: `01-baseline.md`).

## 템플릿

```markdown
# 실험 N차 — <실험명>

- 날짜:
- 변경 내용:
- 사용 데이터(스냅샷/버전):
- 확인 지표: mAP / Precision / Recall / F1 / IDF1 / ID Switch / False All-Clear Rate 중 해당 항목
- 결과:
- 주요 오류 사례:
- 다음 개선 방향:
```

CLAUDE.md 5번(작업 파이프라인) Step 3 순서: 베이스라인 → 가림·원거리 추가 → 저조도·연기 증강 → PPE 추가 → 클래스 불균형 보완 → 오류 데이터 재수집 → 추적 연결 → 관측지속시간 규칙 적용 → 동일 복장·집단 시험 → 임계값 조정.
